## 느낀점

* 아차, java 로 되어있던 코드를 사용하다 보니 중첩함수를 바꾸는 과정이 생략되었다.
  나중에는 책을 보면서 더 작은 단위로 나누는 것을 연습해야겠다.

* 잘 짜여진 코드를 보다 보면 `playFor()` 와 같이 한 라인 짜리 함수를 본 적은 있었으나,
  이렇게 까지 나눠본 적은 없었고, 따라는 해보았으나 실무에서 이렇게 할 수 있을지는 의문이다. 

* thisAmount 를 amountFor() 로 inline 하는 과정도 함수를 두번 사용하는 경우라서, 선뜻 해보기 어려 보인다.